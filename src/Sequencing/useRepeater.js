const useRepeater = () => {
  useEffect(() => {
    //code that starts a sequencer
  }, []);
  //PROBLEM: I need to cobble together the most reliable way of playing syncronized Javascript Audio and MIDI
};

export default useRepeater;
